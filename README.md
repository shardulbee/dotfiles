# dotfiles

Personal machine setup.

## Install

```sh
jj git clone https://github.com/shardulbee/dotfiles dotfiles
./dotfiles/setup
```

## What setup does

- installs Brewfile apps with `brew bundle` on macOS
- links dotfiles into `~/.config` and `~/bin`
- installs CLI tools with `mise`
